import React from 'react';

const BotonComprar = ({ onClick }) => {
  return (
    <button className="boton-comprar" onClick={onClick}>
      Comprar
    </button>
  );
};

export default BotonComprar;
